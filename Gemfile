# frozen_string_literal: true

source "https://rubygems.org"

# gem "rails"
gem "cocoapods"
gem "fastlane"

plugin_path = File.join(File.dirname(__FILE__), 'fastlane', 'Pluginfile')
eval_gemfile(plugin_path) if File.exist?(plugin_path)

#git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

